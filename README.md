# Ml_metods
few metod can be used for instruct a ml pipine

first steps create a venv with
uv venv
activate it with 
source .venv/Scripts/activate 
(I'm working on windows with bash like terminal)
inizialize the venv 
uv init 
install a few packeges
$ uv add numpy pandas scikit-learn matplotlib jupyter ipykernel
